<%- include('comunes/header',{titulo:'Subparcela'}) %>
<br>

<div class="container">
    <div class="solid">
        <% const existeMensaje = typeof mensaje !== "undefined" ? true : false; %>

        <% if(existeMensaje){ %>
            <div class="animated fadeOut alert alert-<%= accion %>" role="alert">
                MENSAJE: <%= mensaje %>
            </div>
        <% } %>

        <% var apellido = 'Cardenas' %>
        <nav>
            <ul class="nav justify-content-end">

                <li class="nav-item">
                    <a href="/Parcela/parcelasSuboparcelas" style="color: black">Ménu Parcela</a>
                </li>

            </ul>

        </nav>
        <h1 align="center"> Lista de Subparcelas</h1>
        <p align="center">Crea, edita, actualiza y busca parcelas. Puedes buscar
            Subparcelas por codigo.
        </p>


        <form action="/Subparcela/parcela" method="get">
            <div align="center">
                <div class="col-sm-9">

                    <div class="form-group">
                        <input id="busqueda"
                               type="text"
                               class="form-control"
                               placeholder="Buscar Sub-parcela"

                               name="busqueda">
                    </div>

                </div>
                <div class="col-sm-3">

                    <button class="btn btn-outline-info btn-block btn-buscar"
                            type="submit">Buscar
                    </button>

                </div>
            </div>
        </form>
        <br>
        <div align="center">
            <div class="col-sm-9"></div>
            <div class="col-sm-3">
                <a class="quitar-decoracion" href="/Subparcela/crear-subparcela">
                    <button class="btn btn-outline-success btn-espaciado btn-block">
                        Crear Subparcela
                    </button>
                </a>
            </div>
        </div>
        <br>

        <table class="table table-striped table-hover animated fadeInUp">
            <caption>Lista de Subarcelas</caption>
            <!-- tr -> table row -->
            <thead>
            <tr align="center">
                <!-- th -> table header -->
                <th width="30%">Código</th>
                <th width="50%">Medidas</th>

            </tr>
            </thead>
            <!-- tr -> table row -->
            <tbody>
            <% arreglo.forEach((subparcela)=>{ %>
                <tr align="center">
                    <!-- td -> table detail -->
                    <td><%= subparcela.codigo %></td>
                    <td><%= subparcela.medidas %></td>


                    <td>
                        <form action="/Subparcela/borrar/<%= subparcela.id %>"
                              method="post"
                        >
                            <!--<button type="button">Yo no</button>-->
                            <!--<button>Yo si</button>-->
                            <button type="submit"
                                    class="btn btn-outline-danger btn-block btn-espaciado"
                                    onclick="borrar(event)">Eliminar
                            </button>
                        </form>
                        <a class="quitar-decoracion" href="/Subparcela/actualizar-subparcela/<%= subparcela.id %>">
                            <button class="btn btn-outline-info btn-block btn-espaciado"
                                    type="button">
                                Actualizar
                            </button>
                        </a>

                    </td>
                </tr>
            <% }) %>
            </tbody>
        </table>


    </div>

</div>

<%- include('comunes/librerias') %>
<%- include('comunes/footer') %>

